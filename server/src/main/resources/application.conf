ktor {
  deployment {
    port = 8080
    watch = [ server, shared/api ]
  }

  application {
    modules = [ com.markstash.server.ApplicationKt.main ]
  }
}

jwt {
  issuer = "markstash"
  audience = "markstash"
  realm = "markstash"
  secret = "bad secret"
}
